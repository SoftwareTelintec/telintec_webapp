import CustomForm from './CustomForm';

function LoginPage() {
	return (
		<section className="h-[calc(100vh-1rem)] flex justify-center items-center w-full">
			<CustomForm />
		</section>
	);
}

export default LoginPage;
